# 目标

我希望实现一个ARM CHI模型，它可以处理CHI协议中的各个事务。

为了验证这个CHI模型，我希望实现一个简易的Cache模型，这个Cache模型需要使用实现的ARM CHI模型，接收和响应对应的CHI事务。

CHI模型和Cache模型是完全解耦的，因为我希望这个CHI模型可以使用源码集成的方式，运用到其他的项目中。

为了验证整个通路，我希望使用GEM5来加载程序执行，GEM5使用ARM架构编译，使用SE模型以及Ruby Cache进行仿真。CPU Core的模拟使用GEM5的能力，包括L1 Cache，但是L2 Cache必须使用我们的Cache模型，并且使用到CHI模型的能力。我们可以在GEM5和自己的L2模型之间，增加一个中间件，负责GEM5 CHI模型和我们自己的CHI模型之间的协议转换。

## 使用的编程语言

- C++ 17